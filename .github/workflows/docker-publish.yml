name: Build and publish Docker images

on:
  push:
    branches: [main]
    paths:
      - 'MeterItProSync/api/**'
      - 'MeterItProSync/frontend/**'
      - 'MeterItProSync/mcp/**'
      - 'MeterItProSync/provisioner/**'
      - 'framework/**'
      - 'version.json'

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        service:
          - name: meteritpro-sync-api
            context: .
            file: MeterItProSync/api/Dockerfile
          - name: meteritpro-sync-frontend
            context: MeterItProSync/frontend
            file: MeterItProSync/frontend/Dockerfile
          - name: meteritpro-sync-mcp
            context: .
            file: MeterItProSync/mcp/Dockerfile
          - name: meteritpro-sync-provisioner
            context: MeterItProSync/provisioner
            file: MeterItProSync/provisioner/Dockerfile

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate version
        id: version
        run: |
          node scripts/generate-version.mjs
          echo "version=$(jq -r .version version.json)" >> $GITHUB_OUTPUT

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push ${{ matrix.service.name }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.service.context }}
          file: ${{ matrix.service.file }}
          push: true
          tags: ${{ env.IMAGE_PREFIX }}/${{ matrix.service.name }}:latest
          build-args: VITE_APP_VERSION=${{ steps.version.outputs.version }}
          cache-from: type=gha,scope=${{ matrix.service.name }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service.name }}
